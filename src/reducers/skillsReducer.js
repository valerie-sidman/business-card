import {
} from '../actions/actionTypes';


const initialState = {
  
}

export default function skillsReducer(state = initialState, action) {
  switch (action.type) {
    // case :
      

    default:
      return state;
  }
}